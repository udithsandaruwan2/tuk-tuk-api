# Sample exports

These small files illustrate shape only. Full simulation rows are produced by `npm run db:seed`.

- `provinces-head.json` — first 3 provinces as returned from the DB (structure reference).
- `location-pings-sample.json` — example ping objects (not from a live run).
