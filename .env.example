# Local development (matches docker-compose.yml defaults)
DATABASE_URL="postgresql://tuk_tuk:tuk_tuk_dev@localhost:5433/tuk_tuk?schema=public"

# Used in later weeks for JWT + device keys
# JWT_SECRET="change-me-in-production"
# DEVICE_PEPPER="change-me-in-production"
