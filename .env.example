# MongoDB connection
MONGODB_URI="mongodb://localhost:27017/tuk_tuk"

# Auth
JWT_SECRET="change-me-in-production"
JWT_EXPIRES_IN="8h"

# API port
PORT=3000

# Host port published by docker compose (container still listens on 3000)
API_PORT=3001
